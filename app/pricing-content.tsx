import { Check } from "lucide-react";
import { CheckoutButton } from "@/components/public/checkout-button";
import { PublicShell } from "@/components/public/public-shell";
import { SectionHeading } from "@/components/public/section-heading";
import { formatPrice, plans } from "@/lib/billing/plans";

export default function PricingContent() { const enabled = Boolean(process.env.STRIPE_SECRET_KEY && process.env.STRIPE_MONTHLY_PRICE_ID && process.env.STRIPE_YEARLY_PRICE_ID); return <PublicShell><main className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><SectionHeading eyebrow="Membership" title="Make every month more meaningful." copy="Prices below are configurable implementation values in GBP, not prices defined by the PRD." />{!enabled && <p className="mt-8 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">Stripe Test Mode is not configured yet. Checkout remains safely unavailable.</p>}<div className="mt-14 grid max-w-4xl gap-5 md:grid-cols-2">{Object.values(plans).map((plan) => { const featured = plan.id === "yearly"; return <article key={plan.id} className={`rounded-3xl border p-8 ${featured ? "border-emerald-600 bg-emerald-700 text-white shadow-xl" : "border-slate-200 bg-white text-slate-950"}`}><p className="text-sm font-semibold uppercase tracking-[.16em] opacity-80">{plan.name}</p><p className="mt-5 text-4xl font-semibold">{formatPrice(plan.unitAmountMinor)}<span className="text-base font-normal"> / {plan.interval}</span></p><p className="mt-4 text-sm leading-6 opacity-80">{plan.description}</p><ul className="mt-8 grid gap-3 text-sm">{["Choose a charity you care about", "Track golf performance", "Monthly draw participation"].map((item) => <li key={item} className="flex gap-3"><Check className="size-5 shrink-0" />{item}</li>)}</ul><CheckoutButton plan={plan.id} enabled={enabled} className={`mt-9 rounded-full px-5 py-3 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-60 ${featured ? "bg-white text-emerald-800" : "bg-slate-950 text-white"}`} /></article>; })}</div></main></PublicShell>; }
