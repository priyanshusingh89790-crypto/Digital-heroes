import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight, Heart } from "lucide-react";
import type { PublicCharity } from "@/lib/public/charities";
export function CharityCard({ charity }: { charity: PublicCharity }) { return <article className="group overflow-hidden rounded-3xl border border-slate-200 bg-white transition duration-300 hover:-translate-y-1 hover:shadow-xl hover:shadow-slate-200/60"><div className="relative flex aspect-[16/9] items-end overflow-hidden bg-gradient-to-br from-emerald-100 via-lime-50 to-amber-100">{charity.image_path ? <Image src={charity.image_path} alt="" fill className="object-cover" /> : <Heart className="m-6 size-10 text-emerald-700/70" aria-hidden="true" />}<span className="absolute right-4 bottom-4 rounded-full bg-white/90 px-3 py-1 text-xs font-medium text-slate-700 backdrop-blur">Community partner</span></div><div className="p-6"><h3 className="text-xl font-semibold text-slate-950">{charity.name}</h3><p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-600">{charity.short_description}</p><Link href={`/charities/${charity.slug}`} className="mt-5 inline-flex items-center gap-1 text-sm font-semibold text-emerald-700">Explore their work <ArrowUpRight className="size-4" /></Link></div></article>; }
